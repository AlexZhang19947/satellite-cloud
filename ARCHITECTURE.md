# 云原生卫星网络可视化系统 - 架构设计文档

## 📋 项目概述

本项目是将原有 Django + Vue.js 卫星网络可视化系统重构为云原生架构的版本，使用 Go 作为后端语言，优化资源消耗，适配 Kubernetes 集群部署。

## 🎯 设计目标

### 核心目标
1. **资源消耗最小化**：使用轻量级框架和优化策略
2. **云原生架构**：完全适配 Kubernetes 15 节点集群
3. **CI/CD 自动化**：GitLab CI/CD 全流程自动化
4. **高性能**：Go 语言带来的性能优势
5. **可扩展性**：支持水平扩展和微服务架构

### 技术选型原则
- **轻量级**：选择资源占用小的框架和工具
- **标准化**：遵循云原生最佳实践
- **易维护**：代码结构清晰，文档完善

## 🏗️ 架构设计

### 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                     Kubernetes Cluster (15 nodes)            │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐  │
│  │   Ingress     │    │   Service    │    │   Service    │  │
│  │  (Nginx/      │    │   (Frontend) │    │   (Backend)  │  │
│  │   Traefik)    │    │              │    │              │  │
│  └──────┬────────┘    └──────┬───────┘    └──────┬───────┘  │
│         │                    │                   │           │
│  ┌──────▼────────────────────▼───────────────────▼───────┐ │
│  │              Pods (Frontend - Nginx Static)             │ │
│  └─────────────────────────────────────────────────────────┘ │
│                                                               │
│  ┌──────┬──────┬──────┬──────┬──────┬──────┐               │
│  │ Pod  │ Pod  │ Pod  │ Pod  │ Pod  │ Pod  │  (Backend)    │
│  │(Go)  │(Go)  │(Go)  │(Go)  │(Go)  │(Go)  │  Replicas     │
│  └──┬───┴──┬───┴──┬───┴──┬───┴──┬───┴──┬───┘               │
│     │      │      │      │      │      │                    │
│  ┌──▼──────▼──────▼──────▼──────▼──────▼──────┐            │
│  │         PostgreSQL (StatefulSet)          │            │
│  │    (Primary + Replicas with Read Replicas) │            │
│  └────────────────────────────────────────────┘            │
│                                                               │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │         ConfigMap / Secret (配置管理)                    │ │
│  └─────────────────────────────────────────────────────────┘ │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

### 技术栈对比

| 组件 | 原方案 | 新方案 | 优势 |
|------|--------|--------|------|
| **后端框架** | Django (Python) | Go + Gin/Echo | 内存占用减少 70-80%，启动速度快 10x |
| **数据库** | PostgreSQL | PostgreSQL (优化) | 保持数据兼容性，优化连接池 |
| **前端** | Vue.js + Vite | Vue.js + Vite (静态构建) | 构建后静态文件，资源占用最小 |
| **Web服务器** | Django Dev Server | Nginx (静态) | 静态文件服务，零运行时开销 |
| **容器镜像** | Python:3.8 (~900MB) | Alpine + Go (~20MB) | 镜像大小减少 95% |
| **运行时内存** | ~200-300MB | ~20-50MB | 内存占用减少 80% |

## 📦 组件设计

### 1. 后端服务 (Go API Server)

#### 框架选择
- **Gin** (推荐) 或 **Echo**：轻量级 HTTP 框架
- **GORM**：ORM 框架，支持 PostgreSQL
- **Viper**：配置管理
- **Zap**：高性能日志库

#### 项目结构
```
backend/
├── cmd/
│   └── server/
│       └── main.go          # 应用入口
├── internal/
│   ├── api/                 # HTTP 处理器
│   │   ├── handlers/        # 请求处理器
│   │   └── middleware/      # 中间件
│   ├── service/             # 业务逻辑层
│   ├── repository/          # 数据访问层
│   ├── model/               # 数据模型
│   └── config/              # 配置管理
├── pkg/                     # 可复用包
│   ├── database/            # 数据库连接
│   ├── logger/              # 日志工具
│   └── validator/           # 验证器
├── migrations/              # 数据库迁移
├── Dockerfile
├── go.mod
└── go.sum
```

#### API 设计
保持与原 API 兼容：
- `GET /api/scenarios/` - 场景列表
- `GET /api/scenarios/{id}` - 场景详情
- `GET /api/scenarios/{id}/satellites` - 场景下的卫星
- `GET /api/satellites/` - 卫星列表（支持过滤）
- `GET /api/satellites/{id}` - 卫星详情

#### 性能优化策略
1. **连接池优化**：PostgreSQL 连接池（最大 10-20 连接）
2. **查询优化**：使用索引，避免 N+1 查询
3. **缓存策略**：Redis 缓存热点数据（可选）
4. **响应压缩**：Gzip 压缩
5. **分页优化**：游标分页替代偏移分页

### 2. 前端服务 (Static Web)

#### 构建策略
- **构建时**：Vite 构建为静态文件
- **运行时**：Nginx 提供静态文件服务
- **资源优化**：代码分割、懒加载、CDN（可选）

#### 部署方式
- **ConfigMap**：存储构建后的静态文件（小项目）
- **Nginx Pod**：使用 Nginx 镜像提供静态服务（推荐）
- **CDN/对象存储**：大规模部署使用（可选）

### 3. 数据库 (PostgreSQL)

#### 部署方案
- **StatefulSet**：保证数据持久化
- **PersistentVolume**：数据持久化存储
- **Read Replicas**：读写分离（可选，根据负载）

#### 优化配置
- 连接池：最大连接数 100
- 查询缓存：启用查询结果缓存
- 索引优化：基于查询模式优化索引

### 4. 配置管理

#### ConfigMap
- 应用配置（非敏感）
- 环境变量
- 前端配置

#### Secret
- 数据库密码
- API 密钥
- TLS 证书

## 🚀 部署架构

### Kubernetes 资源清单

#### Namespace
```yaml
apiVersion: v1
kind: Namespace
metadata:
  name: satellite-system
```

#### Backend Deployment
```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: satellite-api
  namespace: satellite-system
spec:
  replicas: 3  # 根据负载调整
  template:
    spec:
      containers:
      - name: api
        image: registry.gitlab.com/your-group/satellite-cloud/backend:latest
        resources:
          requests:
            memory: "32Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "200m"
```

#### Frontend Deployment
```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: satellite-frontend
  namespace: satellite-system
spec:
  replicas: 2
  template:
    spec:
      containers:
      - name: nginx
        image: nginx:alpine
        resources:
          requests:
            memory: "16Mi"
            cpu: "10m"
          limits:
            memory: "64Mi"
            cpu: "100m"
```

#### Service
```yaml
apiVersion: v1
kind: Service
metadata:
  name: satellite-api
  namespace: satellite-system
spec:
  selector:
    app: satellite-api
  ports:
  - port: 8080
    targetPort: 8080
```

#### Ingress
```yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: satellite-ingress
  namespace: satellite-system
spec:
  rules:
  - host: satellite.example.com
    http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: satellite-api
            port:
              number: 8080
      - path: /
        pathType: Prefix
        backend:
          service:
            name: satellite-frontend
            port:
              number: 80
```

## 📊 资源估算

### 单 Pod 资源需求

| 组件 | CPU Request | CPU Limit | Memory Request | Memory Limit |
|------|-------------|-----------|----------------|--------------|
| Backend (Go) | 50m | 200m | 32Mi | 128Mi |
| Frontend (Nginx) | 10m | 100m | 16Mi | 64Mi |
| PostgreSQL | 100m | 500m | 256Mi | 1Gi |

### 集群资源需求（最小配置）

假设部署：
- Backend: 3 replicas
- Frontend: 2 replicas
- PostgreSQL: 1 primary + 1 replica

**总资源需求：**
- CPU: ~500m (0.5 core)
- Memory: ~800Mi

**15 节点集群容量：**
- 假设每个节点 4 CPU, 8GB RAM
- 总容量: 60 CPU, 120GB RAM
- **资源利用率: < 1%** ✅

## 🔄 CI/CD 流程

### GitLab CI/CD Pipeline

```yaml
stages:
  - build
  - test
  - build-image
  - deploy

# 构建后端
build-backend:
  stage: build
  script:
    - cd backend
    - go build -o bin/server ./cmd/server

# 构建前端
build-frontend:
  stage: build
  script:
    - cd frontend
    - npm ci
    - npm run build

# 构建 Docker 镜像
build-docker:
  stage: build-image
  script:
    - docker build -t $CI_REGISTRY_IMAGE/backend:$CI_COMMIT_SHA ./backend
    - docker push $CI_REGISTRY_IMAGE/backend:$CI_COMMIT_SHA

# 部署到 Kubernetes
deploy-k8s:
  stage: deploy
  script:
    - kubectl set image deployment/satellite-api api=$CI_REGISTRY_IMAGE/backend:$CI_COMMIT_SHA
```

## 🔍 监控和日志

### 监控方案
- **Prometheus**：指标收集
- **Grafana**：可视化面板
- **健康检查**：`/health` 和 `/ready` 端点

### 日志方案
- **结构化日志**：JSON 格式
- **集中日志**：ELK Stack 或 Loki
- **日志级别**：DEBUG, INFO, WARN, ERROR

## 🔐 安全考虑

1. **TLS/HTTPS**：Ingress 配置 TLS
2. **Secret 管理**：使用 Kubernetes Secret
3. **网络策略**：NetworkPolicy 限制 Pod 间通信
4. **RBAC**：最小权限原则
5. **镜像扫描**：CI/CD 中集成安全扫描

## 📈 扩展性设计

### 水平扩展
- Backend：通过增加 replicas 扩展
- Frontend：无状态，轻松扩展
- Database：使用 Read Replicas

### 垂直扩展
- 根据实际负载调整资源 limits

## 🎯 迁移策略

### 阶段 1：并行运行
- 新系统部署到新 namespace
- 数据同步（从旧数据库迁移）
- 逐步切换流量

### 阶段 2：功能验证
- API 兼容性测试
- 性能对比测试
- 用户体验验证

### 阶段 3：完全切换
- 流量完全切换到新系统
- 旧系统保留作为备份
- 监控新系统稳定性

## ❓ 需要确认的问题

在开始实施前，请确认以下信息：

1. **集群信息**
   - [ ] Kubernetes 版本？
   - [ ] Ingress Controller 类型（Nginx/Traefik/其他）？
   - [ ] 存储类（StorageClass）类型？
   - [ ] 是否有私有镜像仓库？

2. **网络配置**
   - [ ] 域名配置？
   - [ ] TLS 证书管理方式（Let's Encrypt/自有证书）？
   - [ ] 是否需要内网访问？

3. **数据迁移**
   - [ ] 现有数据量大小？
   - [ ] 是否需要数据迁移脚本？
   - [ ] 是否需要双写（新旧系统同时写入）？

4. **性能要求**
   - [ ] 预期 QPS（每秒查询数）？
   - [ ] 并发用户数？
   - [ ] 响应时间要求？

5. **监控和日志**
   - [ ] 是否已有 Prometheus/Grafana？
   - [ ] 日志收集方案（ELK/Loki/其他）？
   - [ ] 告警通知方式？

6. **CI/CD**
   - [ ] GitLab 版本和配置？
   - [ ] 是否需要多环境（dev/staging/prod）？
   - [ ] 部署策略（蓝绿/滚动更新）？

## 📝 下一步行动

1. ✅ 确认上述问题
2. ✅ 创建项目结构
3. ✅ 实现 Go 后端基础框架
4. ✅ 配置 Docker 和 Kubernetes
5. ✅ 设置 GitLab CI/CD
6. ✅ 数据迁移脚本
7. ✅ 测试和优化

---

**文档版本**: v1.0  
**最后更新**: 2026-02-07  
**维护者**: 开发团队
