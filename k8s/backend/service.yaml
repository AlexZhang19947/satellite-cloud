apiVersion: v1
kind: Service
metadata:
  name: satellite-api
  namespace: satellite-system
  labels:
    app: satellite-api
spec:
  type: ClusterIP
  selector:
    app: satellite-api
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
